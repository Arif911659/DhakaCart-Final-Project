# 🔙 DhakaCart Backend API

**এটি কি? (What is this?)**
এটি DhakaCart-এর ব্রেইন বা লজিক লেয়ার। এটি `Node.js` এবং `Express` দিয়ে তৈরি।

**কেন এটি দরকার? (Why do we need this?)**
- ফ্রন্টএন্ড থেকে ডাটা রিসিভ করা (যেমন অর্ডার প্লেস করা)।
- ডাটাবেসের (PostgreSQL) সাথে কথা বলা।
- ক্যাশ মেমোরি (Redis) ব্যবহার করে ফাস্ট রেসপন্স দেয়া।

---

## ⚙️ Technical Details

| Component | Technology | Port | Purpose |
|-----------|------------|------|---------|
| **Runtime** | Node.js v18 | **5000** | API Server |
| **Framework**| Express.js | - | Routing & Middleware |
| **Database** | PostgreSQL | 5432 | Persistent Data |
| **Cache** | Redis | 6379 | Session & Product Cache |

---

## 📂 ফাইল স্ট্রাকচার (File Structure)

```
backend/
├── server.js           # মেইন এন্ট্রি পয়েন্ট (Main Entry Point)
├── routes/             # API রাউটস (API Routes)
├── controllers/        # লজিক কন্ট্রোলার (Business Logic)
├── Dockerfile          # কন্টেইনার ইমেজ বিল্ড ফাইল
└── package.json        # ডিপেন্ডেন্সি লিস্ট
```

---

## 🚀 কিভাবে রান করবেন? (How to Run)

### লোকাল মেশিনে (Local):
```bash
npm install
npm start
```

### ডকার দিয়ে (Docker):
```bash
docker build -t dhakacart-backend .
docker run -p 5000:5000 dhakacart-backend
```
