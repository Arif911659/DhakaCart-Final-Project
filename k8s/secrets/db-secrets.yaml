apiVersion: v1
kind: Secret
metadata:
  name: dhakacart-secrets
  namespace: dhakacart
type: Opaque
stringData:
  # Database credentials
  DB_USER: "dhakacart"
  DB_PASSWORD: "dhakacart123"  # Change in production!
  
  # Note: In production, use sealed-secrets or external secrets management
  # For now, this is base64 encoded when applied
