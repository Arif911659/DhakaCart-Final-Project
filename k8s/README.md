# ☸️ Kubernetes Manifests

**এটি কি? (What is this?)**
এখানে আমাদের অ্যাপ্লিকেশনের "State" ডিফাইন করা আছে। অর্থাৎ কয়টা পড চলবে, কিভাবে নেটওয়ার্কিং হবে - সব YAML ফাইলে লেখা।

**কেন এটি দরকার? (Why do we need this?)**
- `kubectl apply` কমান্ড দিয়ে পুরো অ্যাপ্লিকেশন রান করার জন্য।
- কনফিগারেশন (ConfigMap), সিক্রেট (Secrets), এবং সার্ভিস (Service) ম্যানেজ করার জন্য।

---

## 📂 ফোল্ডার স্ট্রাকচার (Folder Structure)

```
k8s/
├── deployments/        # অ্যাপ এবং ডাটাবেস পড (Deployments)
├── services/           # ইন্টারনাল ও এক্সটারনাল নেটওয়ার্কিং (Services)
├── configmaps/         # এনভায়রনমেন্ট ভেরিয়েবল (Config)
├── secrets/            # পাসওয়ার্ড এবং সেন্সিটিভ ডাটা (Encrypted)
├── ingress/            # লোড ব্যালেন্সার রুলস (ALB Ingress)
├── monitoring/         # Prometheus, Grafana, Loki স্ট্যাক
└── enterprise-features/ # Vault, Velero, Cert-Manager (Phase 2)
```

---

## 🚀 কিভাবে ব্যবহার করবেন? (How to Use)

### ১. সব একসাথে ডেপ্লয় করতে:
```bash
./scripts/deploy-4-hour-window.sh
# অথবা
./k8s/deploy-prod.sh
```

### ২. ম্যানুয়ালি অ্যাপ্লাই করতে:
```bash
kubectl apply -f k8s/deployments/
kubectl apply -f k8s/services/
```
